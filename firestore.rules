rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Family tipping app without auth — open read/write (trusted users only)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
